backend:
  name: github
  repo: kcompanyra-ux/Dash
  branch: main
  base_url: https://auth.decapcms.org

# ⚠️ THIS WAS MISSING AND CAUSING THE ERROR
media_folder: "images/uploads"
public_folder: "/images/uploads"

collections:
  - name: settings
    label: Site Settings
    files:
      - name: general
        label: General
        file: content.json
        fields:
          - name: page_title
            label: Page Title
            widget: string
          - name: main_heading
            label: Main Heading
            widget: string
